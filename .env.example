# --- API Server Configuration ---
PORT=7171
HOSTNAME=localhost

# --- PostgreSQL Configuration (Matches docker-compose.yaml) ---
PG_NAME=chat_app_db
PG_URL=localhost
PG_USER=chat_user
PG_SECRET=changethispassword
# Note: External port is 5500 as per docker-compose.yaml
PG_PORT=5500

# --- Hasura Configuration ---
HASURA_URL=http://localhost:8080
HASURA_SECRET=myadminsecretkey

# --- Security & Auth ---
# This MUST match the key inside HASURA_GRAPHQL_JWT_SECRET in docker-compose.yaml
JWT_SECRET=pEdoXw44SkzMnZ+D7B0PRY9sqI6t1bgDPY87OickAgg=

# --- Temporal Configuration ---
# Used for workflow orchestration
TEMPORAL_URL=localhost:7233

# --- AI / LLM Configuration ---
# Required for resume analysis and job ranking
GEMINI_API_KEY=your_gemini_api_key_here

# --- MinIO Configuration (Optional/Advanced) ---
MINIO_URL=http://localhost:9000
MINIO_PORT=9000
MINIO_USR=minioadmin
MINIO_PWD=minioadmin

meta {
  name: loginUser
  type: http
  seq: 3
}

post {
  url: {{awsUrl}}user/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "alexMerc1",
    "password": "abcde1234"
  }
}

script:post-response {
  if(res.status === 200){
    bru.setVar("LoggedInUser", res.body);
    console.log("done");
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

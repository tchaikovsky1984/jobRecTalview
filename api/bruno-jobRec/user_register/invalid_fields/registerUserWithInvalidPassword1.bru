meta {
  name: registerUserWithInvalidPassword1
  type: http
  seq: 2
}

post {
  url: {{baseURL}}user/register
  body: json
  auth: inherit
}

body:json {
  {
    "username": "anshul",
    "email": "sjgnjs@gmail.com",
    "password": "passd",
    "name": "Name"
  }
}

assert {
  res.status: eq 400
  res.body.error: eq invalid password (at least 8 chars)
}

settings {
  encodeUrl: true
  timeout: 0
}

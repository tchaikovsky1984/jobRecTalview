table:
  name: user
  schema: public
array_relationships:
  - name: resumes
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: resume
          schema: public
